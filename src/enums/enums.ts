export enum ESortingTypes {
  POPULAR = 'popular',
  PRICE = 'price',
}

export enum EFilterType {
  CATEGORY = 'category',
  MIN_PRICE = 'minPrice',
  MAX_PRICE = 'maxPrice',
  IS_FAVORITES = 'isFavorites',
}
